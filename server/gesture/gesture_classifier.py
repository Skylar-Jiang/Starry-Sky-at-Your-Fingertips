"""Optional landmark-only gesture classifier stub.

This module intentionally avoids camera/video processing. It exists as a safe
placeholder for future dynamic gesture experiments that receive browser-created
landmarks and trajectories.
"""


def classify_landmark_packet(packet):
    """Return None until an experimental classifier is explicitly added."""
    return None
